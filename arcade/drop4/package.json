{
  "name": "amg-hub-drop4",
  "private": true,
  "type": "module",
  "description": "AMG Hub (nonprofit, educational) edition of Drop4. Vanilla JS + Canvas + three.js. Reuses Drop4's pure rules brain; Hub-native UI + economy. type:module is for Node test runs only — the static site ignores this file.",
  "scripts": {
    "test": "node tests/engine.test.mjs"
  }
}
