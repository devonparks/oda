# Prebuilt dependency bundles: keep the bytes exactly as esbuild wrote them.
# Without this, Git's autocrlf rewrites every line of a 12 MB minified file on
# checkout, which churns the blob and makes diffs meaningless.
* -text
